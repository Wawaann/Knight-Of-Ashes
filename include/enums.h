
#ifndef ENUMS_H
    #define ENUMS_H

    typedef enum menu_one {
        PLAY,
        SETTINGS,
        QUIT
    } menu_one_t;

    typedef enum menu_two {
        MODE,
        SCREEN,
        MUSIC,
        BACK,
        QUIT_SET
    } menu_two_t;

#endif // ENUMS_H