/*
** EPITECH PROJECT, 2022
** B-MUL-200-NCE-2-1-mydefender-maxime.novarese
** File description:
** destroy_all
*/

#include "../include/my.h"

void destroy_all(game_t *game)
{
    // for (int i = 0; game->scn_mn[i]; i++) {
    //     for (int j = 0; game->scn_mn[i]->btn[j]; j++) {
    //         sfSprite_destroy(game->scn_mn[i]->btn[j]->sprt);
    //         sfTexture_destroy(game->scn_mn[i]->btn[j]->txtr);
    //     }
    //     sfSprite_destroy(game->scn_mn[i]->bg->sprt);
    //     sfTexture_destroy(game->scn_mn[i]->bg->txtr);
    // }
}
