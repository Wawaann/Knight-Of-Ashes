/*
** EPITECH PROJECT, 2022
** B-MUL-200-NCE-2-1-mydefender-maxime.novarese
** File description:
** free_mem
*/

#include "../include/my.h"

void free_scn_mnu(scene_mn_t **scene)
{
    // for (int i = 0; scene[i]; i++) {
    //     free(scene[i]->bg);
    //     for (int j = 0; scene[i]->btn[j]; j++)
    //         free(scene[i]->btn[j]);
    //     for (int j = 0; scene[i]->text[j]; j++)
    //         free(scene[i]->text[j]);
    //     free(scene[i]->btn);
    //     free(scene[i]->text);
    //     free(scene[i]);
    // }
    // free(scene);
}

void free_mem(game_t *game)
{
    // free_scn_mnu(game->scn_mn);
    // free(game);
}
